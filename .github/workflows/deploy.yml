name: deploy

on:
  push:
    branches:
      - 27-feature-ci-구축

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        run: npm install --force
        run: npm run build
